# Contributing

Do not modify files under `workflows/frozen_lhende_2026/` in place. Scientific changes should be implemented in `src/amfca/`, tested, documented, and versioned. New thresholds or data-source changes must be explicit configuration changes and must not be silently substituted into exact-reproduction runs.
